<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Recommended Destinations</title>
</head>
<body style="font-family: Arial, sans-serif; margin: 0; padding: 20px; background-color: #f8f8f8; color: #333; text-align: center;">
    <div style="max-width: 1200px; margin: 0 auto;">
        {{#if locations.length}}
            <h1 style="color: #4a90e2;">Recommended Destinations Based on Your Preferences</h1>
            <div style="display: flex; flex-wrap: wrap; justify-content: center; gap: 20px;">
                {{#each locations}}
                <div style="background-color: #fff; padding: 20px; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); border-radius: 8px; width: 300px; transition: transform 0.3s ease-in-out;">
                    <img src="{{image_url}}" alt="Image of {{name}}" style="width: 100%; height: 200px; object-fit: cover; border-radius: 8px;">
                    <div style="margin-top: 15px;">
                        <h2>{{name}}</h2>
                        <p>{{description}}</p>
                        <p><strong>Recommended Budget:</strong> ${{budget_max}} </p>
                        <p><strong>Preferences:</strong> {{preferences}}</p>
                    </div>
                </div>
                {{/each}}
            </div>
        {{else}}
            <h1>No Recommendations Found</h1>
            <p>We could not find any destinations matching your criteria. Please try modifying your search preferences.</p>
        {{/if}}
        <a href="/home" style="color: #4a90e2; text-decoration: none; font-size: 18px; margin-top: 20px; display: inline-block; background-color: #eee; padding: 10px 15px; border-radius: 5px; transition: background-color 0.3s;">Back to Trip Planner</a>
    </div>
    <script>
        document.querySelectorAll('.card').forEach(card => {
            card.addEventListener('mouseover', () => card.style.transform = 'translateY(-10px)');
            card.addEventListener('mouseout', () => card.style.transform = 'none');
        });
    </script>
</body>
</html>
